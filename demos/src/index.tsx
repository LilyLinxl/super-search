import React from 'react';
import { render } from 'react-dom';
import Search from '../../src/Search/AdvancedSearch';
const App = () => (
    <Search />
);
render(<App />, document.getElementById("root"));